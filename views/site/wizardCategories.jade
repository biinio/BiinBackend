div
	.section
		h1(translate="CATEGORIES.TITLE")
		p(translate="CATEGORIES.SUB_TITLE")
	.section
		#categories
			.row
				//
				a.col-md-2.catModule(ng-repeat="cat in categories" ng-click="switchCategoryState(cat)")
					.catModuleWrapper(ng-class="containsCategory(cat)")
						.catIcon(class="icon icon-{{cat.icon}}")
						p {{cat.displayName}}