'use strict';

angular.module('versions',['ui.bootstrap'])
    .controller('versionsController', ['$scope','$http','$modal',
    function($scope, $http, $modal) {

        function onLoad(){

        }

        onLoad();
    }
]);